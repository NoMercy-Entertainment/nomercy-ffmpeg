name: Release

on:
  workflow_run:
    workflows:
      - Test
    types:
      - completed
  workflow_dispatch:
    inputs:
      force_rebuild:
        description: 'Force rebuild of all images'
        required: true
        type: boolean
        default: true
      
jobs:    
  check-build:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.bump-version.outputs.version }}      
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Bump version
        id: bump-version
        run: |
          git config --global --add safe.directory ${{ github.workspace }}
          # Get the latest version tag
          LATEST_TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo "v1.0.0")
          
          echo "LATEST_TAG=$LATEST_TAG" 

          # Extract components
          MAJOR=$(echo "$LATEST_TAG" | cut -d. -f1 | sed 's/v//')
          MINOR=$(echo "$LATEST_TAG" | cut -d. -f2)
          PATCH=$(echo "$LATEST_TAG" | cut -d. -f3)

          # Validate components
          echo "MAJOR=$MAJOR, MINOR=$MINOR, PATCH=$PATCH"

          # Increment patch version
          NEW_PATCH=$(($PATCH + 1))
          NEW_VERSION=v$MAJOR.$MINOR.$NEW_PATCH 
          
          echo "version=$NEW_VERSION" >> $GITHUB_OUTPUT
          echo "New version will be: $NEW_VERSION"
    
  release:
    runs-on: ubuntu-latest
    needs: check-build
    steps:
      - name: Download Release Artifacts
        uses: actions/download-artifact@v4
        with:
          name: release-artifacts
          run-id: ${{ github.event.workflow_run.id }}
          github-token: ${{ secrets.GITHUB_TOKEN }}
          path: downloads/

      - name: Log Downloaded Files
        run: ls -la downloads
        
      - name: Create Release
        id: create-release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ needs.check-build.outputs.version }}
          release_name: ${{ needs.check-build.outputs.version }}
          draft: false
          prerelease: false
          
      - name: Upload Linux x86_64 Release Asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create-release.outputs.upload_url }}
          asset_path: downloads/ffmpeg-8.0-linux-x86_64.tar.gz
          asset_name: ffmpeg-8.0-linux-x86_64-${{ needs.check-build.outputs.version }}.tar.gz
          asset_content_type: application/gzip

      - name: Upload Linux Aarch64 Release Asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create-release.outputs.upload_url }}
          asset_path: downloads/ffmpeg-8.0-linux-aarch64.tar.gz
          asset_name: ffmpeg-8.0-linux-aarch64-${{ needs.check-build.outputs.version }}.tar.gz
          asset_content_type: application/gzip

      - name: Upload Windows x86_64 Release Asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create-release.outputs.upload_url }}
          asset_path: downloads/ffmpeg-8.0-windows-x86_64.zip
          asset_name: ffmpeg-8.0-windows-x86_64-${{ needs.check-build.outputs.version }}.zip
          asset_content_type: application/gzip

      # - name: Upload Windows aarch64 Release Asset
        # uses: actions/upload-release-asset@v1
        # env:
        #   GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        # with:
        #   upload_url: ${{ steps.create-release.outputs.upload_url }}
        #   asset_path: downloads/ffmpeg-8.0-windows-aarch64.tar.gz
        #   asset_name: ffmpeg-8.0-windows-aarch64-${{ needs.check-build.outputs.version }}.tar.gz
        #   asset_content_type: application/gzip

      - name: Upload Darwin x86_64 Release Asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create-release.outputs.upload_url }}
          asset_path: downloads/ffmpeg-8.0-darwin-x86_64.tar.gz
          asset_name: ffmpeg-8.0-darwin-x86_64-${{ needs.check-build.outputs.version }}.tar.gz
          asset_content_type: application/gzip

      - name: Upload Darwin arm64 Release Asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create-release.outputs.upload_url }}
          asset_path: downloads/ffmpeg-8.0-darwin-arm64.tar.gz
          asset_name: ffmpeg-8.0-darwin-arm64-${{ needs.check-build.outputs.version }}.tar.gz
          asset_content_type: application/gzip
